import type { ItemType } from './useItems';
import type { ResizableInfo } from './useResizable';
export default function useResize(items: ItemType[], resizableInfos: ResizableInfo[], percentSizes: number[], containerSize: number, updateSizes: (sizes: number[]) => void): readonly [(index: number) => void, (index: number, offset: number) => number[], () => void, (index: number, type: "start" | "end") => number[], number | undefined];
